# Examples

## 모호한 기능 요청

요청이 "검색을 빠르게 해 달라"면 캐시, 인덱스, 비동기 처리를 한꺼번에 넣지 않습니다. 응답 시간, 동시 처리량, 화면 체감 속도 중 무엇을 개선할지 확인한 뒤 가장 작은 검증 가능한 변경을 고릅니다.

## 버그 수정

요청이 "빈 이메일에서 검증기가 멈춘다"면 먼저 빈 문자열과 공백 문자열을 재현하는 테스트를 만듭니다. 그 테스트를 통과시키는 입력 처리만 바꾸고, 관련 없는 사용자명 검증이나 서식 변경은 건드리지 않습니다.

## 리팩터링

요청이 "정렬 코드를 정리해 달라"면 변경 전 테스트를 통과시키고, 동작을 바꾸지 않는 범위를 정합니다. 변경 후 같은 테스트와 필요한 정적 검사를 실행해 기존 동작이 보존됐음을 확인합니다.
